{
  "name": "xingcheng-plan-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host",
    "preview": "astro preview --host 127.0.0.1 --port 4322",
    "check": "astro check",
    "build": "astro build",
    "build:verify": "npm run check && npm run build && npm test",
    "test": "node --test tests/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "hls.js": "^1.7.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.63.0",
    "typescript": "^6.0.3"
  }
}
